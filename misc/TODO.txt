Stage2.asm

1, **URGENT** need to write a memory detection tool to get lower, upper, and the memory map
2, Write Boot Info Structure(32bitsubs.asm) for export to kernel

MochaFSTool

1, Write Proper Tool (manual develop)

Kernel

1, Get the sp in the location and clear registers
2, Write Basic Debug Tool(stack like console?)
3, Write Memory manager(heap and stdlib mem functions)
4, Start to think about general kernel design and features
